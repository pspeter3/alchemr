.row
  .span12
    .page-header
      %h1
        = h! @website.name
        %small= link_to h!(@website.url), h!(@website.url)
.row
  .span6
    %p= h! @website.desc
  .span6
    = partial 'common/stats', :locals => {:model => @website}
