%table.table.table-striped
  %thead
    %th Title
    %th Active
    %th Max
    %th Questions
    %th Restricted
  %tbody
    - surveys.each do |survey|
      %tr
        %td= link_to h!(survey.title), url(:surveys, :show, :id => survey.id)
        %td= survey.active.to_s.capitalize
        %td= survey.max
        %td= survey.questions.size
        %td= survey.restricted.to_s.capitalize
